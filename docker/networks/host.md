# Trabalhando com network host

`docker run --rm -d --name nginx --network host nginx`

Ao acessar o localhost:80, já funciona o nginx, sem necessidade de fazer a configuração da porta do container para o host.

[⬅️ Voltar para o início](./entendendo-tipos-networks.md)
